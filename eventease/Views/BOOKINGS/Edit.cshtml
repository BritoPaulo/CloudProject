@model eventease.Models.BOOKINGS

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BOOKINGS</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BOOKINGS_ID" />
            <div class="form-group">
                <label asp-for="EVENTS_ID" class="control-label"></label>
                <select asp-for="EVENTS_ID" class="form-control" asp-items="ViewBag.EVENTS_ID"></select>
                <span asp-validation-for="EVENTS_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VENUES_ID" class="control-label"></label>
                <select asp-for="VENUES_ID" class="form-control" asp-items="ViewBag.VENUES_ID"></select>
                <span asp-validation-for="VENUES_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BOOKING_DATE" class="control-label"></label>
                <input asp-for="BOOKING_DATE" class="form-control" />
                <span asp-validation-for="BOOKING_DATE" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
